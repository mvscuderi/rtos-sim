Same as 6 with context switching overhead. 
 Two tasks, two instances, arrival of high priority instance overlaps with the execution of the low priority task. Priority is set according the rate monotonic strategy. Includes context switching overhead
